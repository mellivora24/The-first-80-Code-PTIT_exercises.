#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>
#include <math.h>

int main() {
    // int n;
    // scanf("%d", &n);

    // for (int i=1; i<=n; i++) {
    //     char s[10000];
    //     itoa(i, s, 2);
    //     printf("%s\n", s);
    // }
    char s[2] = "1";
    printf("%04s", s);
    return 0;
}